<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TheApp.Views.MainPage">
  <StackLayout>
    <Label Text="{Binding MainText}" VerticalOptions="Center" HorizontalOptions="Center" />
    <Button Text="Teste a conexão" Clicked="btnConexaoTeste"></Button>
    <Button x:Name="btnSsl" Text="Inserir SSL na conexão" Clicked="btnSSLTeste" IsEnabled="False"></Button>
    <ListView ItemsSource="{Binding Mesas}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <TextCell Text="{Binding nomeMesa}" />
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>


  </StackLayout>



</ContentPage>